server:
  port: 9876
  max-http-header-size: 1048567
  tomcat:
    max-http-post-size: 1048567

threadpool:
  core-pool-size: 50
  max-pool-size: 100
  queue-capacity: 100
  keep-alive-seconds: 300

spring:
  ## mysql配置
  datasource:
    name: druidDataSource
    type: com.alibaba.druid.pool.DruidDataSource
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://11.53.56.97:3306/scps3?useUnicode=true&characterEncoding=UTF-8&autoReconnect=true&failOverReadOnly=false
    username: scps
    password: keHwQW*5XeFLXht#w6uv
  rabbitmq:
    host: 11.53.56.96
    port: 5672
    username: scp
    password: 2h1vjUB!oZ7kU1!TiR9C
    ## druid配置
    druid:
      filters: stat,wall,log4j,config
      max-active: 100
      initial-size: 1
      max-wait: 60000
      min-idle: 1
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: select 'x'
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-open-prepared-statements: 50
      max-pool-prepared-statement-per-connection-size: 20

  jpa:
    show-sql: true

#mybatis-plus
mybatis-plus:
  mapper-locations: classpath*:com/richfit/job/**/*Mapper.xml
  global-config:
    refresh: true
  configuration:
    map-underscore-to-camel-case: true

log-path: d:/logs
app-dir : job
log-level: DEBUG

# 请求超时时间(单位秒)
time_out: 60
# 请求失败的尝试间隔(单位秒)
retry_interval: 10
# 最大尝试次数
max_retry_num: 6

#admin
auth-exception-ip:  iscp-api-admin
auth-exception-port:  7080

# 资产探测
iscp-task-asset-detect:
  ip: iscp-api-asset-detect
  port: 9825
  method: /v3.0.1/assetdetect/createTask

# tasking综合服务
iscp-api-merge:
  ip: iscp-api-merge
  port: 9815
  method: /v3.0.1/vulnerability/createTask